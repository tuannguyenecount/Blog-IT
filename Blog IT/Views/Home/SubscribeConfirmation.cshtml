
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="col-md-12">
    <div class="subscribe-confirmation-page text-center">
        @if (ViewBag.Result == 1)
        {
        <h2 class="text-success">SUCCESS!</h2>
        <h3>Đăng ký nhận tin từ blog thành công</h3>
        <p>
            Kể từ bây giờ chúng tôi sẽ gửi thông báo đến mail mà bạn đã đăng ký mỗi khi blog có bài viết mới. Hãy kiểm tra email thường xuyên nhé :)
        </p>
        }
        @if (ViewBag.Result == 0)
        {
            <p class="text-danger">
                @ViewBag.ErrorMessage
                
            </p>
        }
        @if (ViewBag.Result == 2)
        {
            <h2 class="text-danger">FAILED!</h2>
            <h3>Đăng ký nhận tin từ blog thất bại</h3>
            <p>
                Có vẻ như email mà bạn nhập đã đăng ký nhận tin trước đây rồi. Nếu bạn muốn hủy đăng ký thì vui lòng liên hệ admin
            </p>
        }
    </div>
</div>
